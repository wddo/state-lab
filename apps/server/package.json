{
  "name": "server",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "start": "json-server --watch db.json --port 3001"
  },
  "devDependencies": {
    "json-server": "1.0.0-beta.3"
  }
}
